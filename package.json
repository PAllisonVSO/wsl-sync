{
  "name": "wsl-fsync",
  "version": "0.1.2",
  "description": "WSL2 folder watcher and Windows sync tool",
  "main": "dist/cli.js",
  "files": [
    "dist"
  ],
  "bin": {
    "wsl-fsync": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/cli.ts",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm test"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "commander": "^12.1.0",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "vitest": "^3.1.1"
  }
}
